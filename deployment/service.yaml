apiVersion: v1
kind: Service
metadata:
  name: cdp-quota-customizer-admission-manager
  labels:
    app: cdp-quota-customizer
spec:
  ports:
  - port: 443
    targetPort: 443
  selector:
    app: cdp-quota-customizer
